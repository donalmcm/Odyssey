<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>
        <!-- Database connection settings -->
        <property name="connection.driver_class">com.mysql.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://odysseydb.cqojs0vqjoon.eu-west-1.rds.amazonaws.com:3306/odysseydb</property>
        <property name="connection.username">homer</property>
        <property name="connection.password">myPassword</property>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">10</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.MySQL55Dialect</property>

        <!-- Enable Hibernate's automatic session context management -->
        <property name="current_session_context_class">thread</property>

        <property name="hbm2ddl.auto">update</property>

        <!-- Disable the second-level cache -->
        <!--<property name="cache.provider_class">org.hibernate.cache.internal.NoCacheProvider</property>-->

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>

        <mapping class="com.odyssey.model.Employee"/>
        <mapping class="com.odyssey.model.Odyssey"/>
        <mapping class="com.odyssey.model.Topic"/>
        <mapping class="com.odyssey.model.OdysseyMeeting"/>
        <mapping class="com.odyssey.model.Availability"/>
        <mapping class="com.odyssey.model.Review"/>

    </session-factory>
</hibernate-configuration>
